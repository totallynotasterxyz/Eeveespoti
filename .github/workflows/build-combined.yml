name: Build EeveeSpoti (Liquid Glass + Eevee)

on:
  workflow_dispatch:
    inputs:
      ipa_url:
        description: "Direct link to a decrypted Spotify IPA (use 9.1.78)"
        required: true
        type: string

env:
  THEOS: ${{ github.workspace }}/theos

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout this repo
        uses: actions/checkout@v4

      - name: Checkout official spoti.pw
        uses: actions/checkout@v4
        with:
          repository: skopevoj/spoti.pw
          path: spotipw

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Checkout Theos
        uses: actions/checkout@v4
        with:
          repository: theos/theos
          path: theos
          submodules: recursive

      - name: Install tools
        run: |
          brew install make dpkg ldid
          python3 -m pip install --break-system-packages git+https://github.com/asdfzxcvbn/pyzule-rw.git

      - name: Download Spotify IPA
        run: |
          mkdir -p spotipw/ipa
          echo "Downloading IPA from provided link..."
          curl -fsSL "${{ inputs.ipa_url }}" -o spotipw/ipa/spotify.ipa
          file spotipw/ipa/spotify.ipa
          ls -lh spotipw/ipa/

      - name: Build spoti.pw the official way (make release)
        working-directory: spotipw
        run: |
          make release
          echo "=== spoti.pw output ==="
          ls -lh out/

      - name: Download latest EeveeSpotify
        run: |
          mkdir -p debs
          curl -sL "https://api.github.com/repos/SideloadLabs/EeveeSpotifyReincarnated/releases/latest" -o eevee.json
          python3 -c '
import json
data = json.load(open("eevee.json"))
for a in data["assets"]:
    if "iphoneos-arm64.deb" in a["name"]:
        print("Found:", a["name"])
        print(a["browser_download_url"])
        open("eevee_url.txt", "w").write(a["browser_download_url"])
        break
'
          curl -L -o debs/eevee.deb "$(cat eevee_url.txt)"
          ls -lh debs/

      - name: Inject Eevee on top of official spoti.pw IPA
        run: |
          mkdir -p final
          cyan -i spotipw/out/*.ipa -o final/EeveeSpoti.ipa -f debs/eevee.deb -u -w
          echo "=== Final IPA ==="
          ls -lh final/

      - name: Upload combined IPA
        uses: actions/upload-artifact@v4
        with:
          name: EeveeSpoti-LiquidGlass
          path: final/*.ipa
          retention-days: 5
